-- This migration intentionally has no automatic rollback.
-- It reconciles legacy schema drift to production canonical behavior.
-- Reverting it safely requires a manual, environment-specific plan.
SELECT 1;
